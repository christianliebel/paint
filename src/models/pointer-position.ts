/**
 * Contains the position of the user's pointer.
 */
export interface PointerPosition {
  /**
   * Contains the X position relative to the canvas.
   */
  clientX: number;
  /**
   * Contains the Y position relative to the canvas.
   */
  clientY: number;
}
